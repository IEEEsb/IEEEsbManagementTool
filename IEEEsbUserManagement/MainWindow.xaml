<Window x:Class="IEEEsbUserManagement.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:IEEEsbUserManagement.ViewModel"
        xmlns:converters="clr-namespace:IEEEsbUserManagement.Converters"
        Title="IEEEsb User Management" Height="768" Width="1366">
    <DockPanel>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="140"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Button Background="Transparent" BorderBrush="Transparent" HorizontalAlignment="Left" Click="Button_Click">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20">
                    <TextBlock Text="Administración de Usuarios IEEEsb" FontSize="72" FontWeight="Bold"/>
                    <Image Margin="20,0" Source="pack://application:,,,/Assets/Logo.png"/>
                </StackPanel>
            </Button>
            <Frame x:Name="MainFrame" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" NavigationUIVisibility="Hidden"/>
        </Grid>
    </DockPanel>

</Window>
